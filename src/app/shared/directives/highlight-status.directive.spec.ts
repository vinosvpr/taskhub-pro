import { HighlightStatusDirective } from './highlight-status.directive';

describe('HighlightStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightStatusDirective();
    expect(directive).toBeTruthy();
  });
});
